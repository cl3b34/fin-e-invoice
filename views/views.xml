<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fin-e-invoice.list">
      <field name="name">fin-e-invoice list</field>
      <field name="model">fin-e-invoice.fin-e-invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fin-e-invoice.action_window">
      <field name="name">fin-e-invoice window</field>
      <field name="res_model">fin-e-invoice.fin-e-invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fin-e-invoice.action_server">
      <field name="name">fin-e-invoice server</field>
      <field name="model_id" ref="model_fin-e-invoice_fin-e-invoice"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="fin-e-invoice" id="fin-e-invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fin-e-invoice.menu_1" parent="fin-e-invoice.menu_root"/>
    <menuitem name="Menu 2" id="fin-e-invoice.menu_2" parent="fin-e-invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fin-e-invoice.menu_1_list" parent="fin-e-invoice.menu_1"
              action="fin-e-invoice.action_window"/>
    <menuitem name="Server to list" id="fin-e-invoice" parent="fin-e-invoice.menu_2"
              action="fin-e-invoice.action_server"/>
    -->
  </data>
</odoo>